def reverse_string(name):
    name = list(name)
    left = 0
    right = len(name)-1
    while(left<right):
        name[left],name[right]=name[right],name[left]
        left += 1
        right -= 1

    return "".join(name)
print(reverse_string("sachin"))